%  Pre-run script for RigidTransformsSoln.mlx
% ---- Known Issues     -----
KnownIssuesID = "";
% ---- Pre-run commands -----
 
openExample = @(str)MyOpen(str);

function MyOpen(str)
disp("Open the example " + str)
end